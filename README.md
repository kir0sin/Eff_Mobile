
Этот проект представляет собой Тестовое задание на позицию DevOps.
Здесь представлено простое веб-приложение, состоящее из Python-бэкенда и реверс-прокси на базе Nginx. Все компоненты контейнеризированы с помощью Docker.

## 🛠 Технологии
- **Язык**: Python 3.11 (встроенная библиотека `http.server`)
- **Прокси**: Nginx (Stable Alpine версия)
- **Оркестрация**: Docker Compose
- **Безопасность**: Запуск от не-привилегированного пользователя, сетевая изоляция.

## 🏗 Архитектура и схема взаимодействия
Проект использует классическую схему реверс-проксирования. Бэкенд находится в изолированной внутренней сети Docker и недоступен напрямую с хост-машины.

## 🚀 Инструкция запуска

 1. Подготовка
Убедитесь, что у вас установлены docker и docker-compose.

 2. Запуск приложения
Склонируйте репозиторий и выполните команду в корневой директории проекта:
```
docker-compose up -d --build
```

 3. Команды для проверки работоспособности

Проверка статуса контейнеров и здоровья (Healthcheck):
```
docker-compose ps
```
Убедитесь, что для simple-app-backend указано состояние (healthy).

Проверка ответа через браузер или curl:
```
curl -i http://localhost
```
Вы должны получить HTTP 200 и текст "Hello from Effective Mobile!".

Проверка изоляции порта 8080 (должна быть ошибка подключения):
```
curl -i http://localhost:8080
```

---

 📂 Структура проекта
- backend/ — исходный код Python-приложения и Dockerfile.
- nginx/ — конфигурационный файл прокси-сервера.
- docker-compose.yml — описание сервисов, сетей и правил взаимодействия.
